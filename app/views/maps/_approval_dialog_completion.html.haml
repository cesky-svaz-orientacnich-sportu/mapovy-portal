%p &nbsp;
%div.panel.panel-primary
  %div.panel-heading
    %h4.panel-title Schválit evidovanou mapu nebo vrátit k doplnění
  %div.panel-body
    %p Pokud jsou všechny údaje v pořádku pro definitivní schválení záznamu, lze mapu schválit a záznam uzamknout.
    = form_tag([:authorize_completion, @map], id: "authorize_completion_form") do
      = label_tag "map[blocking_until]", map_attribute_label(:blocking_until, false)
      = select_tag "map[blocking_until]", options_for_select(((Date.today.year..Date.today.year+9).to_a + [0]).sort.reverse.map { |v| [v, v] }, @map.blocking_until)
      %br
      = submit_tag "Schválit", class:'btn btn-primary'
    %hr
    %p Pokud je potřeba údaje doplnit nebo změnit, lze záznam vrátit žadateli (#{print_user @map.created_by, true}) k doplnění.
    = form_tag([:reject_completion, @map], id: "reject_completion_form") do
      %h5 Komentář / zpráva pro žadatele
      = text_area_tag "map[comment]", "", size: "80x8"
      %br
      = submit_tag "Vrátit k doplnění", class:'btn btn-danger'

%h1= t('mapserver.downloadSearchHead')

- if @list and @list.size > 0

  %p= t('mapserver.downloadSearchResults', :count => @list.size)

  %h2= t('mapserver.downExport')

  %ul
    /%li= link_to "KML", :list => @list.join("-"), :format => :kml
    %li= link_to "CSV", :list => @list.join("-"), :format => :csv
    %li= link_to "XLS", :list => @list.join("-"), :format => :xls

  %p= t("mapserver.downNote")
- else
  %p Pravděpodobně došlo k chybě při hledání, zkuste to, prosím, znovu.

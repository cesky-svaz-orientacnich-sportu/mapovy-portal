%h1 Moje mapy vrácené k opravě

- maps = current_user.created_maps.where(state: [Map::STATE_CHANGE_REQUESTED, Map::STATE_FINAL_CHANGE_REQUESTED])
- if maps.exists?

  = render partial: 'list', locals: {maps: maps}

- else

  %p Žádné mapy.